export interface AboutHighlight {
  title: string;
  description: string;
  metrics: string;
}

export interface ExperienceItem {
  company: string;
  role: string;
  period: string;
  stack: string[];
  achievements: string[];
}
